[pytest]
testpaths = src/tests
addopts = -v --tb=short

markers =
    smoke: quick setup verification tests
    fast:  API and unit tests suitable for parallel runs
    slow:  full browser flows, run sequentially
